minio:
  target: prod
  outputs:
    prod:
      type: trino
      threads: 1
      catalog: minio
      schema: landing
      host: trino.warehouse.svc.cluster.local
      port: 8080
      user: trino

iceberg_bronze:
  target: prod
  outputs:
    prod:
      type: trino
      threads: 1
      catalog: iceberg
      schema: bronze
      host: trino.warehouse.svc.cluster.local
      port: 8080
      user: trino

iceberg_silver:
  target: prod
  outputs:
    prod:
      type: trino
      threads: 1
      catalog: iceberg
      schema: silver
      host: trino.warehouse.svc.cluster.local
      port: 8080
      user: trino

iceberg_gold:
  target: prod
  outputs:
    prod:
      type: trino
      threads: 1
      catalog: iceberg
      schema: gold
      host: trino.warehouse.svc.cluster.local
      port: 8080
      user: trino
